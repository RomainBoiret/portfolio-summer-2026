RESEND_API_KEY=re_xxxxxxxx
CONTACT_TO_EMAIL=romain.boiret.1@ens.etsmtl.ca
CONTACT_FROM_EMAIL=Portfolio Contact <onboarding@resend.dev>

# Optional: unlock contribution counts via GitHub GraphQL (read-only fine-grained or classic PAT).
# Without it, repos / stars / languages still load over the public REST API.
GITHUB_TOKEN=

# Headless WordPress (Hostinger). Leave empty to use local Markdown + src/data.
# Example: https://cms.romainboiret.com
WP_URL=

# Shared secret for POST /api/revalidate (must match Portfolio CMS → Revalidate secret).
WP_REVALIDATE_SECRET=
